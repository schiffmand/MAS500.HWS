# Print out all the state names from the csv# Coded in the "imperative" stylef = open('2012_US_election_state.csv', 'r')print "Opened file:"obama_tot=0romney_tot=0all_lines = f.readlines()for line in all_lines:    columns = line.split(",")    print columns[1]print "done ("+str(len(all_lines))+" lines)"for line in all_lines:    columns = line.split(",")    if not(isinstance(columns[3],str)):    	obama_tot = obama_tot + columns[3]    	romney_tot = romney_tot + columns[5]    	print obama_totprint romney_tot